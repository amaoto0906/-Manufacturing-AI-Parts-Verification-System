# === 部品照合システム / Python 依存パッケージ ===
# コア（必須・軽量）
numpy>=1.26
pillow>=10.0
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.1
python-multipart>=0.0.9
jinja2>=3.1
faiss-cpu>=1.7

# 本番バックボーン（torchvision/DINOv2）は別途インストールする（CUDA版の巨大DLを避けるため）。
# CPU 環境（推奨）:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# ※ torch 無しでも classic バックボーン（numpy/PIL のみ）で全機能が動作する。
# torch>=2.1
# torchvision>=0.16

# 開発・テスト
pytest>=8.0
httpx>=0.27
requests>=2.31
